<UserControl x:Class="AccountingApp.Views.Firme.FirmeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header & Actions -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Upravljanje firmama i preduzećima" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                <TextBlock Text="Pregled celokupnog registra firmi, ažuriranje matičnih podataka i izbor trenutno aktivne firme." FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="➕ Nova firma" Style="{StaticResource ActionButtonStyle}" Height="38" Padding="18,0" Click="BtnNovaFirma_Click" Margin="0,0,8,0"/>
                <Button Content="🔄 Osveži" Height="38" Padding="14,0" Background="#F1F5F9" Foreground="#475569" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" Click="BtnOsvezi_Click"/>
            </StackPanel>
        </Grid>

        <!-- Filter / Search Bar -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12,10" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtPretraga" Height="34" VerticalContentAlignment="Center" Padding="8,0"
                         Text="" TextChanged="TxtPretraga_TextChanged"/>
                <!-- Placeholder tekst kad je prazan -->
                <TextBlock Text="🔍 Pretraži po nazivu, PIB-u ili šifri firme..." Foreground="{StaticResource TextMutedBrush}"
                           IsHitTestVisible="False" VerticalAlignment="Center" Margin="10,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=TxtPretraga}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtUkupno" Text="Ukupno: 0 firmi" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid list of Firme -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid x:Name="DgFirme" AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionMode="Single" GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0" HeadersVisibility="Column"
                      BorderThickness="0" Background="Transparent"
                      RowHeight="42" ColumnHeaderHeight="38"
                      SelectionChanged="DgFirme_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Šifra" Binding="{Binding Sifra}" Width="90"/>
                    <DataGridTextColumn Header="Naziv preduzeća / firme" Binding="{Binding Naziv}" Width="2*"/>
                    <DataGridTextColumn Header="PIB" Binding="{Binding Pib}" Width="110"/>
                    <DataGridTextColumn Header="Matični br." Binding="{Binding MaticniBroj}" Width="110"/>
                    <DataGridTextColumn Header="PTT i Mesto" Binding="{Binding PttIMesto}" Width="140"/>
                    <DataGridTextColumn Header="Adresa" Binding="{Binding Adresa}" Width="1.5*"/>
                    <DataGridTextColumn Header="Žiro račun" Binding="{Binding ZiroRacun}" Width="160"/>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#DCFCE7"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Background" Value="#FEE2E2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Aktivna"/>
                                                <Setter Property="Foreground" Value="#15803D"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="Neaktivna"/>
                                                        <Setter Property="Foreground" Value="#B91C1C"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Action buttons column -->
                    <DataGridTemplateColumn Header="Akcije" Width="230">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Button Content="✅ Izaberi" Height="28" Padding="10,0" Margin="0,0,4,0"
                                            Background="#2563EB" Foreground="White" BorderThickness="0" FontSize="11" FontWeight="SemiBold" Cursor="Hand"
                                            Click="BtnIzaberi_Click"/>
                                    <Button Content="✏️ Izmeni" Height="28" Padding="10,0" Margin="0,0,4,0"
                                            Background="#F1F5F9" Foreground="#334155" BorderThickness="0" FontSize="11" FontWeight="SemiBold" Cursor="Hand"
                                            Click="BtnIzmeni_Click"/>
                                    <Button Content="🗑️ Briši" Height="28" Padding="10,0"
                                            Background="#FEF2F2" Foreground="#991B1B" BorderThickness="0" FontSize="11" FontWeight="SemiBold" Cursor="Hand"
                                            Click="BtnBrisi_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
