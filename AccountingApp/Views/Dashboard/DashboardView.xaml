<UserControl x:Class="AccountingApp.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,24">
            <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,12,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="POSTED ENTRIES" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock x:Name="TxtUkupnoNaloga" Text="339" FontSize="28" FontWeight="Bold" Foreground="#2563EB" Margin="0,6,0,0"/>
                    <TextBlock Text="5.606 journal line items" FontSize="12" Foreground="#10B981" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="12" Padding="20" Margin="6,0,6,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="CHART OF ACCOUNTS" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock x:Name="TxtUkupnoKonta" Text="42" FontSize="28" FontWeight="Bold" Foreground="#059669" Margin="0,6,0,0"/>
                    <TextBlock Text="Active accounts in database" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="12" Padding="20" Margin="6,0,6,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="MATERIAL STOCK ITEMS" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock x:Name="TxtUkupnoMaterijala" Text="2.466" FontSize="28" FontWeight="Bold" Foreground="#D97706" Margin="0,6,0,0"/>
                    <TextBlock Text="105 warehouse locations" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="12" Padding="20" Margin="12,0,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="SUBLEDGER PARTNERS" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock x:Name="TxtUkupnoPartnera" Text="4" FontSize="28" FontWeight="Bold" Foreground="#7C3AED" Margin="0,6,0,0"/>
                    <TextBlock Text="Customers &amp; Vendors" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,12,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DockPanel>
                    <TextBlock Text="Recent Journal Entries" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,16"/>
                    <DataGrid x:Name="DgRecentNalozi" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Entry #" Binding="{Binding BrojNaloga}" Width="110"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding DatumNaloga, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Opis}" Width="*"/>
                            <DataGridTextColumn Header="Debit (RSD)" Binding="{Binding UkupnoDuguje, StringFormat=\{0:N2\}}" Width="130"/>
                            <DataGridTextColumn Header="Credit (RSD)" Binding="{Binding UkupnoPotrazuje, StringFormat=\{0:N2\}}" Width="130"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Company Information" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                    
                    <TextBlock Text="Company Name:" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock Text="ARHIBEL - 2026" FontSize="14" FontWeight="SemiBold" Foreground="#0F172A" Margin="0,2,0,12"/>

                    <TextBlock Text="Address:" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock Text="Srpskih vladara 106, 18300 Pirot" FontSize="13" Foreground="#0F172A" Margin="0,2,0,12"/>

                    <TextBlock Text="Bank Account:" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock Text="160-11508-84" FontSize="13" Foreground="#0F172A" Margin="0,2,0,12"/>

                    <TextBlock Text="Migration Status:" FontWeight="Bold" Foreground="#64748B"/>
                    <Border Background="#ECFDF5" CornerRadius="6" Padding="10,6" Margin="0,6,0,0">
                        <TextBlock Text="✅ Legacy DBF imported to SQLite!" FontSize="12" FontWeight="SemiBold" Foreground="#047857"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
