<UserControl x:Class="AccountingApp.Views.Bilansi.BilansiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="🏛️ Zvanični Finansijski Izveštaji za APR" FontSize="22" FontWeight="Bold" Foreground="#1E3A8A"/>
            <TextBlock Text="Obračun i štampa Bilansa Stanja i Bilansa Uspeha po zvaničnim AOP pozicijama i šemama RS" FontSize="13" Foreground="#64748B" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Filter bar -->
        <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Datum za Bilans Stanja:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                <DatePicker Grid.Column="1" x:Name="DpDatumStanja" Width="130" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <TextBlock Grid.Column="2" Text="Period za Bilans Uspeha:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                    <DatePicker x:Name="DpOdDatuma" Width="120"/>
                    <TextBlock Text=" do " VerticalAlignment="Center" Margin="4,0"/>
                    <DatePicker x:Name="DpDoDatuma" Width="120"/>
                </StackPanel>

                <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="📜 Poreski Bilans (PB-1 / PDP / OA)" Click="BtnOtvoriPoreskiBilans_Click" Padding="14,6" Margin="0,0,8,0" Background="#0D9488" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" ToolTip="Obračun poreza na dobit, usklađivanje rashoda PB-1, PDP i poreska amortizacija OA">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="🔄 Osveži obračun" Click="BtnOsvezi_Click" Padding="12,6" Background="#2563EB" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tabovi za Zvanične APR Izveštaje -->
        <TabControl Grid.Row="2" Background="White">
            <!-- TAB 1: BILANS STANJA -->
            <TabItem Header="🏛️ Bilans Stanja (Aktiva / Pasiva)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtStatusRavnoteze" Text="⚡ Provera ravnoteže u toku..." FontWeight="Bold" FontSize="13" Foreground="#059669"/>
                        </StackPanel>
                        <Button DockPanel.Dock="Right" Content="🖨️" ToolTip="Štampaj Bilans Stanja (PDF)" Click="BtnStampajStanjePdf_Click" Padding="0" Width="36" Background="#059669" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button DockPanel.Dock="Right" Content="X" Click="BtnExportExcelBilansStanja_Click" Padding="0" Width="36" Margin="0,0,8,0" Background="#059669" Foreground="White" FontWeight="Bold" FontSize="20" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" ToolTip="Izvoz u Excel">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </DockPanel>

                    <DataGrid Grid.Row="1" x:Name="DgBilansStanja" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="AOP" Binding="{Binding AopCode}" Width="70">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Pozicija Bilansa Stanja" Binding="{Binding Naziv}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Konta" Binding="{Binding OpsegKonta}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#64748B"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Tekuća godina (RSD)" Binding="{Binding IznosTekucaGodina, StringFormat={}{0:N2}}" Width="160">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- TAB 2: BILANS USPEHA -->
            <TabItem Header="📈 Bilans Uspeha (Prihodi / Rashodi / Rezultat)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtNetoRezultat" Text="📊 Obračun rezultata..." FontWeight="Bold" FontSize="13" Foreground="#2563EB"/>
                        </StackPanel>
                        <Button DockPanel.Dock="Right" Content="🖨️" ToolTip="Štampaj Bilans Uspeha (PDF)" Click="BtnStampajUspehPdf_Click" Padding="0" Width="36" Background="#059669" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button DockPanel.Dock="Right" Content="X" Click="BtnExportExcelBilansUspeha_Click" Padding="0" Width="36" Margin="0,0,8,0" Background="#059669" Foreground="White" FontWeight="Bold" FontSize="20" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" ToolTip="Izvoz u Excel">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </DockPanel>

                    <DataGrid Grid.Row="1" x:Name="DgBilansUspeha" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="AOP" Binding="{Binding AopCode}" Width="70">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Pozicija Bilansa Uspeha" Binding="{Binding Naziv}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Konta" Binding="{Binding OpsegKonta}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#64748B"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Tekuća godina (RSD)" Binding="{Binding IznosTekucaGodina, StringFormat={}{0:N2}}" Width="160">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- TAB 3: STATISTIČKI IZVEŠTAJ (SI) -->
            <TabItem Header="📋 Statistički izveštaj (SI)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="📊 Statistički podaci za APR i Poresku upravu" FontWeight="Bold" FontSize="13" Foreground="#0284C7" VerticalAlignment="Center"/>
                        <Button DockPanel.Dock="Right" Content="X" Click="BtnExportExcelSI_Click" Padding="0" Width="36" Background="#059669" Foreground="White" FontWeight="Bold" FontSize="20" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" ToolTip="Izvoz u Excel"/>
                    </DockPanel>
                    <DataGrid Grid.Row="1" x:Name="DgStatistickiIzvestaj" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="AOP" Binding="{Binding Aop}" Width="70" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Opis statističke pozicije" Binding="{Binding Opis}" Width="*"/>
                            <DataGridTextColumn Header="Konto" Binding="{Binding KontoGrupa}" Width="100"/>
                            <DataGridTextColumn Header="Tekuća god." Binding="{Binding IznosTekuca, StringFormat={}{0:N2}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- TAB 4: TOKOVI GOTOVINE (CASH FLOW) -->
            <TabItem Header="💵 Tokovi gotovine (Cash Flow)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="💵 Izveštaj o tokovima gotovine (Poslovne, investicione i finansijske aktivnosti)" FontWeight="Bold" FontSize="13" Foreground="#0D9488" VerticalAlignment="Center"/>
                        <Button DockPanel.Dock="Right" Content="X" Click="BtnExportExcelCashFlow_Click" Padding="0" Width="36" Background="#059669" Foreground="White" FontWeight="Bold" FontSize="20" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" ToolTip="Izvoz u Excel"/>
                    </DockPanel>
                    <DataGrid Grid.Row="1" x:Name="DgCashFlow" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="AOP" Binding="{Binding Aop}" Width="70" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Pozicija priliva i odliva" Binding="{Binding Opis}" Width="*"/>
                            <DataGridTextColumn Header="Tip aktivnosti" Binding="{Binding TipAktivnosti}" Width="120"/>
                            <DataGridTextColumn Header="Prilivi (RSD)" Binding="{Binding Priliv, StringFormat={}{0:N2}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Odlivi (RSD)" Binding="{Binding Odliv, StringFormat={}{0:N2}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Neto tok (RSD)" Binding="{Binding NetoPrilivOdliv, StringFormat={}{0:N2}}" Width="140" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- TAB 5: PROMENE NA KAPITALU -->
            <TabItem Header="📈 Promene na kapitalu">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="📈 Izveštaj o promenama na kapitalu u toku godine" FontWeight="Bold" FontSize="13" Foreground="#7C3AED" VerticalAlignment="Center"/>
                        <Button DockPanel.Dock="Right" Content="X" Click="BtnExportExcelKapital_Click" Padding="0" Width="36" Background="#059669" Foreground="White" FontWeight="Bold" FontSize="20" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" ToolTip="Izvoz u Excel"/>
                    </DockPanel>
                    <DataGrid Grid.Row="1" x:Name="DgPromeneNaKapitalu" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="AOP" Binding="{Binding Aop}" Width="70" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Opis promene" Binding="{Binding Opis}" Width="*"/>
                            <DataGridTextColumn Header="Osnovni kapital" Binding="{Binding OsnovniKapital, StringFormat={}{0:N2}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Rezerve" Binding="{Binding Rezerve, StringFormat={}{0:N2}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Neraspoređena dobit" Binding="{Binding NerasporedjenaDobit, StringFormat={}{0:N2}}" Width="150" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Gubitak" Binding="{Binding Gubitak, StringFormat={}{0:N2}}" Width="130" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                            <DataGridTextColumn Header="Ukupno kapital" Binding="{Binding UkupnoKapital, StringFormat={}{0:N2}}" Width="150" ElementStyle="{StaticResource NumericColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
