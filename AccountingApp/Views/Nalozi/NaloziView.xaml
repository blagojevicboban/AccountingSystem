<UserControl x:Class="AccountingApp.Views.Nalozi.NaloziView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16,12" Margin="0,0,0,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔍 Search:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                    <TextBox x:Name="TxtPretraga" Width="250" Height="34" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtPretraga_TextChanged"/>
                    <CheckBox x:Name="ChkSamoProknjizeni" Content="Posted entries only" IsChecked="True" VerticalAlignment="Center" Margin="16,0,0,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                </StackPanel>

                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Content="➕ New Entry" Style="{StaticResource ActionButtonStyle}" Margin="0,0,10,0" Click="BtnNoviNalog_Click"/>
                    <Button Content="✅ Post Entry" Style="{StaticResource ActionButtonStyle}" Background="#10B981" Click="BtnProknjizi_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="📖 General Ledger Journal Entries" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgNalozi" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" SelectionChanged="DgNalozi_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Entry #" Binding="{Binding BrojNaloga}" Width="120"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding DatumNaloga, StringFormat=\{0:dd.MM.yyyy\}}" Width="110"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding VrstaNaloga}" Width="120"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Opis}" Width="*"/>
                        <DataGridTextColumn Header="Debit (RSD)" Binding="{Binding UkupnoDuguje, StringFormat=\{0:N2\}}" Width="140"/>
                        <DataGridTextColumn Header="Credit (RSD)" Binding="{Binding UkupnoPotrazuje, StringFormat=\{0:N2\}}" Width="140"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding IsKnjizen}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#E2E8F0"/>

        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="16" Margin="0,16,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock x:Name="TxtDetailHeader" Text="📋 Journal Line Items" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                <DataGrid x:Name="DgStavke" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Line #" Binding="{Binding RedniBroj}" Width="60"/>
                        <DataGridTextColumn Header="Account" Binding="{Binding BrojKonta}" Width="110"/>
                        <DataGridTextColumn Header="Document / Description" Binding="{Binding Opis}" Width="*"/>
                        <DataGridTextColumn Header="Debit (RSD)" Binding="{Binding Duguje, StringFormat=\{0:N2\}}" Width="140"/>
                        <DataGridTextColumn Header="Credit (RSD)" Binding="{Binding Potrazuje, StringFormat=\{0:N2\}}" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
